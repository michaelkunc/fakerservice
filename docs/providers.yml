Returns a list of credit card providers.
---
parameters:
  - name: limit
    in: path
    type: integer
    required: false
    default: 100
    description: Number of results to return
definitions:
  providers:
    type: object
responses:
  200:
    description: A list of providers.
    schema:
      $ref: '#/definitiona/providers'
    examples:
      provider: "VISA 16 digit"
