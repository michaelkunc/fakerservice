Returns a list of fake SSN's.
---
parameters:
  - name: limit
    in: path
    type: integer
    required: false
    default: 100
    description: Number of results to return
definitions:
  ssn:
    type: object
responses:
  200:
    description: A list of emails.
    schema:
      $ref: '#/definitiona/ssn'
    examples:
      ssn: '091-91-6512'