Returns a list of fake phone numbers.
---
parameters:
  - name: limit
    in: path
    type: integer
    required: false
    default: 100
    description: Number of results to return
definitions:
  phone_numbers:
    type: object
responses:
  200:
    description: A list of phone numbers.
    schema:
      $ref: '#/definitions/phone_numbers'
    examples:
      phone_number: '107.235.7374x8251'