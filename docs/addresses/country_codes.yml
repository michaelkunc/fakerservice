Returns a list of country codes.
---
parameters:
  - name: limit
    in: path
    type: integer
    required: false
    default: 100
    description: Number of results to return
definitions:
  country_codes:
    type: object
responses:
  200:
    description: A list of country_codes.
    schema:
      $ref: '#/definitions country_codes'
    examples:
      country_code: "SO"