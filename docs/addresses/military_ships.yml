Returns a list of military ships.
---
parameters:
  - name: limit
    in: path
    type: integer
    required: false
    default: 100
    description: Number of results to return
definitions:
  military_ships:
    type: object
responses:
  200:
    description: A list of military_ships.
    schema:
      $ref: '#/definitions military_ships'
    examples:
      military_state: "USS"
