Returns a list of crypto_currency codes.
---
parameters:
  - name: limit
    in: path
    type: integer
    required: false
    default: 100
    description: Number of results to return
definitions:
  crypto_currency:
    type: object
responses:
  200:
    description: A list of crypto_currency.
    schema:
      $ref: '#/definitiona/crypto_currency'
    examples:
      crypto_currency: "PPC"