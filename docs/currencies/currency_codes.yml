Returns a list of currency codes.
---
parameters:
  - name: limit
    in: path
    type: integer
    required: false
    default: 100
    description: Number of results to return
definitions:
  currency:
    type: object
responses:
  200:
    description: A list of currency.
    schema:
      $ref: '#/definitiona/currency'
    examples:
      currency: "USD"