Returns a list of fake ip addresses
---
parameters:
  - name: limit
    in: path
    type: integer
    required: false
    default: 100
    description: Number of results to return
definitions:
  ip:
    type: object
responses:
  200:
    description: A list of fake image urls.
    schema:
      $ref: '#/definitiona/ip'
    examples:
      ip: "30.87.247.166"