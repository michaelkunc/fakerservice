Returns a list of fake urls.
---
parameters:
  - name: limit
    in: path
    type: integer
    required: false
    default: 100
    description: Number of results to return
definitions:
  url:
    type: object
responses:
  200:
    description: A list of urls.
    schema:
      $ref: '#/definitiona/url'
    examples:
      uri: "https://campos-campbell.com/"