Returns a list of fake passwords
---
parameters:
  - name: limit
    in: path
    type: integer
    required: false
    default: 100
    description: Number of results to return
definitions:
  password:
    type: object
responses:
  200:
    description: A list of fake passwords.
    schema:
      $ref: '#/definitiona/passwords'
    examples:
      password: "o(10HPp_b7"