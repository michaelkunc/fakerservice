Returns a list of fake mac addresses.
---
parameters:
  - name: limit
    in: path
    type: integer
    required: false
    default: 100
    description: Number of results to return
definitions:
  mac_address:
    type: object
responses:
  200:
    description: A list of mac addresses.
    schema:
      $ref: '#/definitiona/mac_address'
    examples:
      mac_address: "10:e0:35:07:31:0c"