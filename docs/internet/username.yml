Returns a list of fake usernames.
---
parameters:
  - name: limit
    in: path
    type: integer
    required: false
    default: 100
    description: Number of results to return
definitions:
  username:
    type: object
responses:
  200:
    description: A list of fake usernames.
    schema:
      $ref: '#/definitiona/username'
    examples:
      username: "michael10"