Returns a lorem word.
---
parameters:
  - name: limit
    in: path
    type: integer
    required: false
    default: 10
    description: Number of results to return
definitions:
  word:
    type: object
responses:
  200:
    description: A list of lorem word.
    schema:
      $ref: '#/definitions word'
    examples:
      word: 'distinctio'