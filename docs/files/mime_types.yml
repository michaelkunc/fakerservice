Returns a list of mime types.
---
parameters:
  - name: limit
    in: path
    type: integer
    required: false
    default: 100
    description: Number of results to return
definitions:
  mime_types:
    type: object
responses:
  200:
    description: A list of mime_types.
    schema:
      $ref: '#/definitiona/mime_types'
    examples:
      mime_type: "message/partial"