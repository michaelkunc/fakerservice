Returns a list of fake expiration dates.
---
parameters:
  - name: limit
    in: path
    type: integer
    required: false
    default: 100
    description: Number of results to return
definitions:
  expiration_date:
    type: object
responses:
  200:
    description: A list of credit card expiration dates.
    schema:
      $ref: '#/definitions/expiration_date'
    examples:
      expiration_date: "11/20"
